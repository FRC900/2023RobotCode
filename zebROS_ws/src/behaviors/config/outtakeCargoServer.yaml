actionlib_cargo_outtake_params:
    outtake_timeout: 2 #timeout for the elevator when outtaking
    pause_time_between_pistons: 1 #time to pause between piston actuations (relevant for cargo outtake), in seconds
    pause_before_elevator_lower: 1 #time to pause after outtaking before lowering the elevator back to default height
    roller_power: -1.0
