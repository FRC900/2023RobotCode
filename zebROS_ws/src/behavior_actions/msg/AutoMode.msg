Header header
int8 auto_mode #not a uint8 b/c hard to get those in rospy
# would like to refactor to string if autos have a name (e.g super().__init__(display_name="Test4Note", # must match choreo path name)