# goal
bool single_step
bool reset # if true, restarts at the first state (by default, preempt = pause). Used for debugging.
---
# result
bool success
---
# feedback
uint8 state
uint8 rung # 0 = ground, 1 = medium, 2 = high, 3 = traversal
